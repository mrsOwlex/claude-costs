{
  "name": "agent-costs",
  "version": "2.0.0",
  "description": "CLI for analyzing AI coding agent usage logs and estimating API-equivalent costs",
  "type": "module",
  "bin": {
    "agent-costs": "./bin/agent-costs.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test 'test/**/*.test.ts'",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0"
  },
  "license": "MIT",
  "dependencies": {
    "better-sqlite3": "^12.10.0"
  }
}
