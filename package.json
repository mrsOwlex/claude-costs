{
  "name": "claude-costs",
  "version": "1.0.0",
  "description": "CLI for analyzing Claude Code usage logs and estimating API-equivalent costs",
  "type": "module",
  "bin": {
    "claude-costs": "./bin/claude-costs.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test 'test/**/*.test.ts'",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0"
  },
  "license": "MIT"
}
